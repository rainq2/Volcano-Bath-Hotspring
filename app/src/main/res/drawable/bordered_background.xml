<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" />
    <corners android:radius="8dp" /> <!-- 圆角半径，可根据需要调整 -->
    <stroke
        android:width="2dp"
    android:color="@android:color/black" />
</shape>
